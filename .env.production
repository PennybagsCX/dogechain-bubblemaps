# Production environment variables
# This file is used by Vite during production builds
# API is now on the same domain as the frontend (relative paths work automatically)
# VITE_API_BASE_URL is left empty to use window.location.origin
