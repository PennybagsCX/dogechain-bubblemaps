#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Increment build number
echo "📦 Incrementing beta build number..."
npm run increment-build

# Stage the updated build-metadata.json
git add build-metadata.json

# Run lint-staged
npx lint-staged

# Run type-check
echo "📝 Running TypeScript type check..."
npm run type-check

echo "✅ Pre-commit checks passed!"
